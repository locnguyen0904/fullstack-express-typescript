import { z } from 'zod';

import { registry } from '@/config/openapi.config';

export const create{{pascalCase name}}Schema = registry.register(
  'Create{{pascalCase name}}',
  z.object({
    // TODO: Add your validation fields here
    name: z.string().openapi({ example: 'My {{pascalCase name}}' }),
  })
);

export const update{{pascalCase name}}Schema = registry.register(
  'Update{{pascalCase name}}',
  z.object({
    // TODO: Add your update validation fields here
    name: z.string().optional().openapi({ example: 'Updated {{pascalCase name}}' }),
  })
);
