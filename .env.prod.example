# Production Environment Variables
# Copy this file to .env.prod and fill in your actual values
#
# For Google Cloud Run deployment, secrets are managed via Secret Manager.
# See docs/DEPLOYMENT.md for setup instructions.

# Database
# Docker Compose: include credentials in URL
# Cloud Run: stored in Secret Manager as "database-url"
DATABASE_URL=mongodb://admin:change-me-strong-password@mongo:27017/backend-template?authSource=admin&replicaSet=rs0&directConnection=true
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=change-me-strong-password

# Redis
# Docker Compose: include password in URL
# Cloud Run: stored in Secret Manager as "redis-url" (use Memorystore IP)
REDIS_URL=redis://:change-me-redis-password@redis:6379
REDIS_PASSWORD=change-me-redis-password

# JWT
# Cloud Run: stored in Secret Manager as "jwt-secret"
JWT_SECRET=change-me-jwt-secret-key-at-least-32-characters
JWT_ACCESS_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=30

# Encryption (min 32 chars, defaults to JWT_SECRET if not set)
# Cloud Run: stored in Secret Manager as "encryption-key"
ENCRYPTION_KEY=change-me-encryption-key-at-least-32-characters

# Admin Seed
# Cloud Run: ADMIN_PASSWORD stored in Secret Manager as "admin-password"
ADMIN_NAME="Super Admin"
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-admin-password

# Application
NODE_ENV=production
PORT=3000

# CORS (comma-separated origins)
# Cloud Run: set to your frontend Cloud Run URL
ALLOWED_ORIGINS=https://your-frontend-url.run.app

# Logging
LOG_LEVEL=info
LOG_DIR=logs

# Frontend
# Cloud Run: set to your backend Cloud Run URL (baked at build time)
VITE_API_URL=https://api.your-domain.com
